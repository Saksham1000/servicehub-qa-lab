[pytest]
testpaths = app/backend/tests automation
addopts = -ra --strict-markers
markers =
    smoke: critical build confidence tests
    regression: broader regression tests
    ui: Selenium browser tests
    api: REST API tests
    database: persistence and integrity tests
    graphql: GraphQL endpoint tests
    security: authentication and authorization tests
    responsive: responsive mobile-web tests
